// =============================================================================
// Ocean decorators — visual elements that wrap or accompany a node
// =============================================================================
// All ports of v1 experimental components (`src/components/_experiments/`).
// None are wired into the runtime today; they live here so the catalog screen
// can showcase them and so we have a single home if we promote any to a real
// status overlay or collaboration affordance later.

export { SecurityPerimeter } from './SecurityPerimeter'
export { OverflowHalo } from './OverflowHalo'
export { PendingWritesBadge } from './PendingWritesBadge'
export { ScannerRover } from './ScannerRover'
export { PresencePin, PresencePinStack, type PresenceUser } from './PresencePin'
